#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a,b;
    printf("enter the value=");
    scanf("%d",&a);
    if(a%2==0)
        printf("the number is even");
    else
        printf("the number is odd");
     printf("\nenter the value=");
    scanf("%d",&b);
    if(b%2==0)
        printf("the number is even");
    else
        printf("the number is odd");
    return 0;
}
